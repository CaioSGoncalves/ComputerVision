# install anaconda
# conda in env-var: export PATH=~/anaconda3/bin:$PATH)
# (não precisei): conda remove opencv
# (não precisei): conda install -c menpo opencv
pip install --upgrade pip
pip install opencv-python or pip install opencv-contrib-python
# https://pypi.org/project/opencv-contrib-python/